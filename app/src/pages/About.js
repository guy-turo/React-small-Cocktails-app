import React from 'react'

const About = () => {
  return (
   <section className='section about-section'>
    <h1 className='section-title'>about us</h1>
    <p>
    Oh, yeah! It perfectly answers the question. However, make sure to notice the change in component to 'element', otherwise will result in a silent warning in the browser console "Matched leaf route at location "/" does not have an element. This means it will render an 
    </p>
   </section>
  )
}

export default About
